# Teiko-Technical-Assessment
Teiko Technical Assessment project: end-to-end pipeline and dashboard for immune cell count data. Uses SQLite + Python to load cell-count.csv, compute relative frequencies, compare responders vs non-responders, run subset analyses, and visualize results in Streamlit.
